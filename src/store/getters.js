const url = state => state.app.url
const token = state => state.app.token
const role = state => state.app.role
const loggedIn = state => state.app.loggedIn
const username = state => state.app.username
const userData = state => state.app.userData
const title = state => state.app.title


export {
  url,
  token,
  role,
  loggedIn,
  username,
  userData,
  title,
}